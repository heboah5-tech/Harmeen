import { useState } from "react";
import { motion } from "framer-motion";
import { User, Phone, ChevronDown, ChevronLeft, Clock, Check, AlertCircle } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";

const STEPS = [
  { label: "التذاكر", icon: "✓", done: true },
  { label: "التفاصيل", icon: "2", active: true },
  { label: "الشراء", icon: "3", done: false },
  { label: "الدفع", icon: "4", done: false },
];

const NATIONALITIES = ["سعودي", "مصري", "أردني", "إماراتي", "كويتي", "بحريني", "عراقي", "يمني", "سوري", "أخرى"];
const ID_TYPES = ["الهوية الوطنية / الإقامة", "جواز السفر"];

function StepBar() {
  return (
    <div className="flex items-center justify-center gap-0 bg-background border-b border-border py-3 px-4 sticky top-0 z-30" dir="rtl">
      {STEPS.map((step, i) => (
        <div key={i} className="flex items-center">
          <div className={`flex flex-col items-center gap-1 px-3 sm:px-5`}>
            <div className={`w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold border-2 transition-all ${
              step.done ? "bg-primary border-primary text-primary-foreground" :
              step.active ? "bg-secondary border-secondary text-secondary-foreground" :
              "bg-background border-border text-muted-foreground"
            }`}>
              {step.done ? <Check className="w-4 h-4" /> : step.icon}
            </div>
            <span className={`text-[10px] font-medium ${step.active ? "text-secondary" : step.done ? "text-primary" : "text-muted-foreground"}`}>
              {step.label}
            </span>
          </div>
          {i < STEPS.length - 1 && (
            <div className={`w-8 sm:w-12 h-0.5 mb-4 ${i === 0 ? "bg-primary" : "bg-border"}`} />
          )}
        </div>
      ))}
    </div>
  );
}

function PassengerForm({ index, label }) {
  const [form, setForm] = useState({ title: "اختار", firstName: "", lastName: "", dob: "", nationality: "", idType: "الهوية الوطنية / الإقامة", idNumber: "", phone: "" });

  const set = (key, val) => setForm(prev => ({ ...prev, [key]: val }));

  return (
    <div className="bg-background border border-border rounded-2xl overflow-hidden mb-4" dir="rtl">
      {/* Header */}
      <div className="flex items-center justify-between px-5 py-4 border-b border-border bg-muted/30">
        <div className="w-7 h-7 rounded-full bg-primary text-primary-foreground flex items-center justify-center text-sm font-bold">
          {index}
        </div>
        <h3 className="font-bold text-foreground text-base">{label}</h3>
      </div>

      <div className="p-5 space-y-5">
        {/* Personal Info */}
        <div>
          <p className="text-xs font-bold text-muted-foreground mb-3 text-end">• المعلومات الشخصية</p>

          {/* Title + Name row */}
          <div className="grid grid-cols-3 gap-3 mb-3">
            <div>
              <label className="text-xs text-muted-foreground block mb-1 text-end">اسم العائلة *</label>
              <input
                value={form.lastName}
                onChange={e => set("lastName", e.target.value)}
                placeholder="أدخل اسم العائلة"
                className="w-full border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background"
              />
            </div>
            <div>
              <label className="text-xs text-muted-foreground block mb-1 text-end">الاسم الأول *</label>
              <input
                value={form.firstName}
                onChange={e => set("firstName", e.target.value)}
                placeholder="أدخل الاسم الأول"
                className="w-full border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background"
              />
            </div>
            <div>
              <label className="text-xs text-muted-foreground block mb-1 text-end">اللقب *</label>
              <div className="relative">
                <select
                  value={form.title}
                  onChange={e => set("title", e.target.value)}
                  className="w-full border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background appearance-none cursor-pointer"
                >
                  <option>اختار</option>
                  <option>أ.</option>
                  <option>د.</option>
                  <option>م.</option>
                </select>
                <ChevronDown className="absolute left-2 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground pointer-events-none" />
              </div>
            </div>
          </div>

          {/* Date of Birth */}
          <div className="mb-3">
            <label className="text-xs text-muted-foreground block mb-1 text-end">تاريخ الميلاد *</label>
            <p className="text-xs text-muted-foreground text-end mb-1.5 flex items-center justify-end gap-1">
              <span>(12+) ألا يقل عن 12 أو أكثر</span>
              <AlertCircle className="w-3 h-3 text-primary" />
            </p>
            <input
              type="date"
              value={form.dob}
              onChange={e => set("dob", e.target.value)}
              className="w-full border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background"
              placeholder="أدخل تاريخ الميلاد"
            />
          </div>

          {/* Nationality */}
          <div className="mb-3">
            <label className="text-xs text-muted-foreground block mb-1 text-end">الجنسية *</label>
            <div className="relative">
              <select
                value={form.nationality}
                onChange={e => set("nationality", e.target.value)}
                className="w-full border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background appearance-none cursor-pointer"
              >
                <option value="">اختار الجنسية</option>
                {NATIONALITIES.map(n => <option key={n}>{n}</option>)}
              </select>
              <ChevronDown className="absolute left-2 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground pointer-events-none" />
            </div>
          </div>
        </div>

        {/* ID Info */}
        <div>
          <p className="text-xs font-bold text-muted-foreground mb-3 text-end">• وثيقة الهوية</p>

          {/* ID Type Toggle */}
          <div className="mb-3">
            <label className="text-xs text-muted-foreground block mb-1.5 text-end">نوع وثيقة الهوية</label>
            <div className="flex rounded-xl border border-border overflow-hidden">
              {ID_TYPES.map((type, i) => (
                <button
                  key={i}
                  type="button"
                  onClick={() => set("idType", type)}
                  className={`flex-1 py-2.5 text-xs font-semibold transition-all ${
                    form.idType === type
                      ? "bg-primary text-primary-foreground"
                      : "bg-background text-foreground hover:bg-muted"
                  }`}
                >
                  {type}
                </button>
              ))}
            </div>
          </div>

          {/* ID Number */}
          <div>
            <label className="text-xs text-muted-foreground block mb-1 text-end">
              {form.idType === "جواز السفر" ? "رقم جواز السفر *" : "الهوية الوطنية / الإقامة الرقم *"}
            </label>
            <input
              value={form.idNumber}
              onChange={e => set("idNumber", e.target.value)}
              placeholder={`أدخل رقم ${form.idType}`}
              className="w-full border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background"
            />
          </div>
        </div>

        {/* Contact Info */}
        <div>
          <p className="text-xs font-bold text-muted-foreground mb-3 text-end">• معلومات الاتصال</p>
          <div className="flex items-center gap-2">
            <input
              value={form.phone}
              onChange={e => set("phone", e.target.value)}
              placeholder="05XXXXXXXX"
              className="flex-1 border border-border rounded-xl px-3 py-2.5 text-sm text-end focus:outline-none focus:ring-2 focus:ring-primary/30 bg-background"
              dir="ltr"
            />
            <div className="w-10 h-10 rounded-xl border border-border bg-muted/30 flex items-center justify-center flex-shrink-0">
              <Phone className="w-4 h-4 text-muted-foreground" />
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

function BookingSummary() {
  return (
    <div className="bg-background border border-border rounded-2xl p-5 mt-4" dir="rtl">
      <h3 className="font-bold text-foreground text-base mb-4 text-end">ملخص الحجز</h3>

      <div className="mb-3 pb-3 border-b border-border/50">
        <p className="text-xs text-muted-foreground text-end mb-2">رحلة المغادرة</p>
        <div className="flex justify-between text-sm text-end">
          <span className="font-bold">138.26 ر.س</span>
          <span className="text-muted-foreground">البالغين (الأساسية)</span>
        </div>
        <div className="flex justify-between text-sm mt-1">
          <span className="text-primary text-xs">× 138.26</span>
          <span className="text-xs text-muted-foreground text-end">البالغين 1</span>
        </div>
      </div>

      <div className="space-y-2 text-sm text-end">
        <div className="flex justify-between">
          <span className="font-semibold">138.26 ر.س</span>
          <span className="text-muted-foreground">الإجمالي قبل الضريبة</span>
        </div>
        <div className="flex justify-between">
          <span className="font-semibold">20.74 ر.س</span>
          <span className="text-muted-foreground">الضريبة (15٪)</span>
        </div>
        <div className="flex justify-between">
          <span className="font-semibold">0.00 ر.س</span>
          <span className="text-muted-foreground">الخرب</span>
        </div>
        <div className="flex justify-between border-t border-border pt-2 mt-2">
          <span className="font-extrabold text-base text-primary">159 ر.س</span>
          <span className="font-bold text-foreground">الإجمالي</span>
        </div>
      </div>
    </div>
  );
}

export default function PassengerDetails() {
  const [progress] = useState(0);
  const navigate = useNavigate();

  return (
    <div className="min-h-screen bg-muted/30" dir="rtl">
      <StepBar />

      <div className="max-w-2xl mx-auto px-4 py-6">
        {/* Page Header */}
        <motion.div
          initial={{ opacity: 0, y: -10 }}
          animate={{ opacity: 1, y: 0 }}
          className="bg-background border border-border rounded-2xl p-5 mb-4"
        >
          <div className="flex items-start justify-between gap-4">
            <div className="text-start">
              <div className="w-12 h-12 rounded-xl bg-muted border border-border flex items-center justify-center mb-2">
                <div className="text-center">
                  <div className="text-xs font-bold text-foreground">0/1</div>
                  <div className="text-[9px] text-muted-foreground">مسافر</div>
                  <div className="text-[9px] text-muted-foreground">كاملة</div>
                </div>
              </div>
            </div>
            <div className="flex-1 text-end">
              <div className="flex items-center justify-end gap-2 mb-2">
                <h2 className="text-lg font-extrabold text-foreground">تفاصيل المسافرين</h2>
                <div className="w-8 h-8 rounded-full bg-primary/10 flex items-center justify-center">
                  <User className="w-4 h-4 text-primary" />
                </div>
              </div>
              <p className="text-xs text-muted-foreground leading-relaxed">
                يرجى تقديم معلومات كاملة لكل مسافر في حالة وجود أكثر من مسافر بالأسواء من أجل تسهيل عملية تسجيل المسافر التالي.
              </p>
            </div>
          </div>

          {/* Progress Bar */}
          <div className="mt-4">
            <div className="flex justify-between text-xs text-muted-foreground mb-1">
              <span>{progress}٪</span>
              <span>نسبة الانتهاء</span>
            </div>
            <div className="w-full h-1.5 bg-muted rounded-full">
              <div className="h-full bg-primary rounded-full transition-all duration-500" style={{ width: `${progress}%` }} />
            </div>
          </div>
        </motion.div>

        {/* Passenger Forms */}
        <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.1 }}>
          <PassengerForm index={1} label="البالغين 1" />
        </motion.div>

        {/* Booking Summary */}
        <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.2 }}>
          <BookingSummary />
        </motion.div>

        {/* Warning */}
        <div className="mt-4 p-4 bg-primary/5 border border-primary/20 rounded-xl flex items-start gap-3 text-end">
          <div className="flex-1">
            <p className="text-xs text-foreground font-medium">
              • التفاصيل غير مكتملة
            </p>
            <p className="text-xs text-muted-foreground mt-1">
              يرجى إكمال تفاصيل 1 مسافر (مسافر إضافية لإكمالها)
            </p>
          </div>
          <AlertCircle className="w-4 h-4 text-primary flex-shrink-0 mt-0.5" />
        </div>

        {/* Submit Button */}
        <button
          onClick={() => navigate("/SeatSelection")}
          className="w-full mt-4 bg-secondary text-secondary-foreground py-4 rounded-xl font-bold text-base flex items-center justify-center gap-2 hover:bg-secondary/90 hover:-translate-y-0.5 hover:shadow-lg transition-all duration-300"
        >
          <ChevronLeft className="w-5 h-5" />
          تأكيد التفاصيل
        </button>

        {/* Bottom timer info */}
        <div className="mt-4 flex items-center justify-between text-xs text-muted-foreground px-1">
          <Link to="/SearchResults" className="text-primary font-medium hover:underline flex items-center gap-1">
            <ChevronLeft className="w-3 h-3 rotate-180" />
            العودة للنتائج
          </Link>
          <div className="flex items-center gap-1">
            <Clock className="w-3 h-3" />
            <span>14:24 دقيقة متبقية</span>
          </div>
        </div>
      </div>
    </div>
  );
}